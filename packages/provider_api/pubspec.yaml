name: provider_api
description: Abstract interface for chat providers, domain models, and events.
version: 0.0.1
resolution: workspace
publish_to: none

environment:
  sdk: ">=3.10.7 <4.0.0"

dependencies:
  copy_with_extension: ^11.0.0
  equatable: ^2.0.5
  json_annotation: ^4.10.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
  build_runner: ^2.4.0
  json_serializable: ^6.12.0
  copy_with_extension_gen: ^11.0.0
