## 0.0.1

Initial release of `provider_api`.

- Added `IChatProvider` abstraction.
- Added core models: `Conversation`, `Message`, `ProviderSession`, `Pagination`, `SendResult`.
- Added provider event types.
- Added provider exception hierarchy.
