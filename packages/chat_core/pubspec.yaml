name: chat_core
description: Domain logic, use-cases, and state machines for chat functionality.
version: 0.0.1
resolution: workspace
publish_to: none

environment:
  sdk: ">=3.10.7 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Local
  provider_api: any

  # Third Party
  bloc: ^9.2.0
  flutter_bloc: ^9.1.1
  riverpod: ^3.2.1
  flutter_riverpod: ^3.2.1
  equatable: ^2.0.8
  copy_with_extension: ^11.0.0
  fast_immutable_collections: ^11.1.0
  meta: ^1.8.0
  rxdart: ^0.28.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  copy_with_extension_gen: ^11.0.0
  test: ^1.24.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.0
